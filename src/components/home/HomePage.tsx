import * as React from "react";

// A extremely simple function-style component (not a class-style component)
const HomePage: () => JSX.Element = (): JSX.Element =>
    (
        <div>
            Home Page
        </div>
    );

export default HomePage;
