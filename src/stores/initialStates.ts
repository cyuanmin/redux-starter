import {ICourse} from "../models/course";
import {IAuthor} from "../models/author";

export const defaultCourses: Array<ICourse> = [];
export const defaultAuthors: Array<IAuthor> = [];
export const defaultAjaxCalls: number = 0;