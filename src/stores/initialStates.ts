import {ICourse} from "../models/course";
import {IAuthor} from "../models/author";

export const defaultCourses: Array<ICourse> = []; // No course by default
export const defaultAuthors: Array<IAuthor> = []; // No authors by default
export const defaultAjaxCalls: number = 0; // No ajax calls by default